#!/bin/sh

NAME=supervisor
DAEMON=/mnt/system/usr/bin/supervisor

start() {
    printf 'Starting Supervisor Service: '
    PID="`pidof $DAEMON`"

    if [ "$PID" ]; then
        echo "$NAME is already running."
    else
        $DAEMON >/dev/null 2>&1 &
        [ $? = 0 ] && echo "OK" || echo "FAIL"
    fi
}

stop() {
    printf 'Stopping Supervisor Service: '
    PID="`pidof $DAEMON`"

    if [ "$PID" ]; then
        kill $PID
        [ $? = 0 ] && echo "OK" || echo "FAIL"
    else
        echo "$NAME is not running."
    fi
}

restart() {
    stop
    start
}

case "$1" in
    start|stop|restart)
        "$1";;
    *)
        echo "Usage: $0 {start|stop|restart}"
        exit 1
esac
